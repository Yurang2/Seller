# 구현 상태 · 2026-09-18 (검토 반영 + M2)

## 2026-09-19 · 우선순위 1~4 (계산기 구매대행 모드 · 주문 M3 · 재확인 Job · 고지 문구 정정)

- **1) 계산기 구매대행 모드**: `calculate()`가 `businessModel`을 받는다. `purchase_agency`면 관세·부가세를 판매자 착지원가에서 빼고 `payer: "customer"` 참고 라인으로만 남기며, `customer_tax_per_unit`을 따로 낸다. USD 환율이 기록돼 있을 때만 미화 150달러 목록통관 초과를 경고하고, 없으면 "환율 필요"만 알린다(추정으로 경고하지 않는다). `costingInput()`이 설정의 business_model과 최신 USD 환율을 읽어 넘긴다. 화면에 모드 배지·소비자 부담 표기. 테스트 `tests/domain/costing.test.ts`.
- **2) 주문(M3)**: `orders` 기록(접수→발주→발송→통관→배송완료→정산/취소/반품). **통관부호 자체는 저장하지 않고 수집 여부만** 남긴다(`customs_code_collected`). 근거는 고객 결제액·공급처 실결제액·배대지 결제액·채널 정산액·환불액이며 금액 근거는 캡처가 있어야 확인이 된다. 파생 작업 3종(접수 1일 경과 → 발주, 발송인데 송장 없음 → 송장, 배송완료 14일 경과·미정산 → 정산). 절차 `orders_manual`을 주문 기록 기준으로 갱신.
- **3) 재확인 요약 Job**: `job_runs` 테이블 + cron `0 23 * * *`(08:00 KST) + `src/api/jobs/digest.ts`. 기한 지난 근거·할 일·처리 대기 주문·막힘을 집계해 기록만 하고 데이터는 바꾸지 않는다. `RESEND_API_KEY`(secret)와 `DIGEST_EMAIL`(var)이 둘 다 있을 때만 메일을 보내고 없으면 "미설정"으로 남는다. 수동 실행 `POST /api/v1/jobs/daily-digest/run`. 홈 지표에 마지막 요약 표시. 테스트 `tests/api/m3.test.ts`.
- **4) 고지 문구 정정(중요)**: 국가기술표준원 「안전관리대상 전기용품의 표시」(https://www.kats.go.kr/content.do?cmsid=227, 2026-09-19 확인)는 구매대행 고지 문구를 **지정 문구 그대로** 쓰도록 한다. 기존 키트의 의역("국내 KC 인증을 받지 않았습니다")을 지정 문구 2줄로 교체하고, **상품별 개별 고지·묶음 고지 불가** 조건을 추가했다. 갱신한 키트 PDF를 무캐릭터 프로필 6개의 표시 항목 답변에 다시 첨부하고 출처를 국가기술표준원으로 바꿨다.
- **못 한 것(정직하게)**: `law.go.kr`·`safetykorea.kr`·스마트스토어 판매자센터 도움말은 이 실행 환경에서 브라우저·curl 모두 차단돼 **공식 화면 캡처를 만들지 못했다.** 어린이제품·KC(위험도 높음)와 채널 정책 항목은 판정하지 않고 두었다. 내가 쓴 요약본을 법령 근거인 것처럼 첨부하지 않는다(R-03). 대신 두 할 일에 캡처 대상·확인할 문장을 구체적으로 적어 두었다.
- 검증: typecheck, 테스트 111개(도메인 54 + Workers API 25 + 데스크톱 32), 빌드 통과. 원격 D1에 `0004_orders_jobs.sql` 적용 후 배포. 배포 뒤 홈 응답 200·비로그인 401·요약 Job 1회 실행 확인.

## 2026-09-19 · 입력 폼 압축 · 보류 규칙 · 운영 데이터 정리(구매대행 전환)

- 입력 폼: 기록 수정/새 기록이 자동 열(230px 기준) 그리드, 36px 입력, 비어 있는 긴 칸은 한 줄로 접힘, "이유 + 저장"은 폼 아래 붙는 유리 액션 바(모바일은 탭 위). 상세 보기도 같은 밀도.
- 규칙: 보류·거절·종료 상품만 쓰는 요건 프로필은 `profile_gate_unknown` 작업을 만들지 않는다(상품이 돌아오면 다시 생긴다). 로컬 테스트로 확인.
- 운영 데이터(모두 API를 거쳐 감사 로그 남김, `wrangler dev --remote` + `.dev.vars` 우회로 실행): 핑구 상품 4개 보류(재검토 2026-12-01), 사업 준비 "사업 모델 결정" 완료(D-01 구매대행), 핑구 전용 수동 할 일 3개 취소·엑셀 이관 완료, 배대지 2곳(이지타오·더싼) 업체·요율표 등록 + 공식 배송비용안내 캡처 첨부 + 무게 구간 가격 근거(확인) + 국내배송 포함(확인) + 이지타오 통관수수료(추정, 제3자). 남은 열린 할 일: 환율 소스, 채널 수수료 캡처.
- 상세페이지 프로세스: `src/domain/listingKit.ts`(고지 블록·금지 표현·이미지 체크리스트·초안 생성, 기록에 없는 값은 [확인 필요]), 상품 화면 "상세페이지 초안" 패널(마크다운 복사), 업무 절차 "상세페이지 만들기 (수동)" 시드, `docs/LISTING_KIT.md` + PDF(`templates/research/listing_kit_2026-09-19.pdf`). 키트 PDF를 무캐릭터 프로필 6개의 표시·반품 답변에 첨부해 통과 판정(조건 이행 작업 12개 해소). 홈 "다음 행동"은 같은 제목의 파생 할 일을 ×N으로 묶는다. 테스트 `tests/domain/listingKit.test.ts`.
- 후보 상품 조사 v1: `docs/RESEARCH_CANDIDATES_2026-09-19.md`. 워크북 111행을 조각 가져오기로 적용(실패 0), 무캐릭터 프로필 6개 × 13항목 중 낮음·법령 근거 항목은 API로 통과/조건부 판정.
- 주의: `wrangler dev --remote`는 `dist/`의 빌드 결과를 쓴다. 코드 수정 후 `pnpm build` 없이 재시작하면 옛 코드가 돈다(이번에 한 번 헛돌았음).

## 2026-09-19 · 리디자인 v3 반영(리퀴드 글래스 · 무채색) + 표 보기 + 모바일 탭·캡처

목업(사용자님 승인, v3)을 그대로 구현했다. 코드 구조는 유지하고 `src/web/theme.css`가 기존 `styles.css` 뒤에서 덮어쓴다.

- 껍데기: 데스크톱은 떠 있는 유리 레일(홈·상품·캡처·할 일·근거·더 보기), 모바일(≤760px)은 유리 하단 탭 5개(홈·상품·캡처·할 일·더 보기). 나머지 화면은 `/more`에 모았다. 상단 바 제거.
- 톤: Apple 시스템 회색 팔레트, 채색은 링크·주요 버튼의 블루와 "막힘" 알약의 오렌지 틴트 둘뿐. 등급 A~D는 검정→회색→흰 테두리, 통과·완료·확인은 검정 알약. 폰트는 프리텐다드(제목·본문) + Inter(숫자·영문), CDN 동적 서브셋.
- 표 보기: 모든 기록 목록이 44px 행 단위 표가 기본(`src/web/pages/Table.tsx`). 종류별 요약 열 3개(참조·선택지·숫자 우선), 상태 알약, 상품은 등급 열, 열린 할 일 수, 갱신일. 카드·단계별(상품) 보기는 세그먼트로 전환하며 기기별 localStorage에 기억한다. 좁은 화면에서는 두 줄 목록으로 바뀐다.
- 홈: 지표 4개(판매 개시까지·막힘·재확인 기한·상품 등급 분포) + "다음 행동" 표(막힘 먼저, 열기 버튼) + "캡처 올리고 근거로 연결" 버튼.
- 캡처 화면(`/capture`): 기록 종류 → 기록 → 값 항목을 고르면 기존 근거 편집기(첨부 업로드·확인/추정 규칙 그대로)가 열린다. 기존 근거가 있으면 그것을 수정한다.
- 개발 편의: `APP_ENV=development/desktop`이면 `/auth/status`도 인증됨으로 응답해 로컬에서 로그인 화면이 뜨지 않는다(API 우회 조건과 동일).
- 검증: typecheck·테스트 96개·빌드 통과, 로컬 Playwright 스크린샷으로 데스크톱 1280/모바일 390 화면 확인 후 배포. 샌드박스에서는 폰트 CDN이 막혀 대체 글꼴로 확인했다.

## 2026-09-19 · 트랙 C-1 완료 · Cloudflare 배포 + 비밀문구 로그인

주소: **https://seller.a98763969.workers.dev** (단일 Worker, 정적 자산 포함, `run_worker_first: ["/api/*"]`). 계정 `7c915f536fa7c52317bc29abed91a540`.

| 항목 | 실제 확인 결과 |
|---|---|
| D1 `seller-db`(5c241e6d…) 원격 마이그레이션 | `0001_core.sql`은 이미 적용돼 있었고 `0002_research.sql`·`0003_listings.sql`을 이번에 원격 적용(49개 명령 성공) |
| R2 버킷 | `seller-attachments`, `seller-backups` 신규 생성(Standard) |
| Worker 배포 | `pnpm deploy`(tsc → vite build → wrangler deploy) 성공. 바인딩 DB·ATTACHMENTS·BACKUPS·ASSETS, vars `APP_ENV=production`, `AUTH_MODE=passphrase` |
| `SESSION_SECRET` | 62자 난수를 생성해 `wrangler secret put`으로 등록. 값은 어디에도 기록하지 않음 |
| 비로그인 차단 | `GET /api/v1/workspace` → 401 `LOGIN_REQUIRED`. `GET /` → 200 HTML(로그인 화면). `GET /api/v1/auth/status` → `configured:false` |
| CSRF | 다른 Origin에서 `POST /api/v1/auth/setup` → 403 `ORIGIN_REJECTED` |
| 배포 직후 결함 | 첫 비밀문구 설정이 500으로 실패. 원인: Workers WebCrypto의 PBKDF2 반복 상한 100,000회(코드 150,000회). 로컬 workerd는 상한을 강제하지 않아 테스트가 잡지 못했다. 100,000회로 낮춰 재배포하고 운영에서 설정→로그인 왕복을 curl로 확인한 뒤 확인용 설정 행은 삭제 |
| 배포 직후 결함 2 | "초기 조사 기록 불러오기"가 500으로 실패. 원인: Workers 요청당 D1 호출 상한 1,000회를 시드 한 번(약 1,080회)이 초과. 마지막 단계 직전까지는 저장됨(상품 4·근거 121·노트 23). 조치: 시드·엑셀 가져오기를 10행 조각으로 나눠 반복 호출(IMP-12), 진행률 표시. 조각당 호출 수 500 미만을 테스트로 고정 |
| 미실시 | 실제 브라우저에서 비밀문구 설정 → 로그인 → 초기 조사 불러오기 → 첨부 업로드(R2) 왕복. 사용자님이 첫 접속 때 진행하면 확인됨 |

로그인 방식은 Cloudflare Access 대신 앱 자체 비밀문구(PBKDF2-SHA256 100k(Workers 상한), HMAC 서명 쿠키 30일, 5회 실패 시 15분 잠금, 로그아웃 시 세션 버전 증가)다. `AUTH_MODE=access`로 바꾸면 기존 Access JWT 검사가 다시 켜진다. 배포 전 로컬 검증: 도메인 49 + Workers API 18 + 데스크톱 25 = 92개 테스트 통과, `tests/api/auth.test.ts` 포함.

## 2026-09-19 · 트랙 A 작업 보존 / 모바일 우선 재계획 대기

사용자님의 휴대폰 사용 요구에 따라 데스크톱 작업을 중단하고 현재 코드와 인계 자료를 보존한다. **트랙 A 완료가 아니며 B·C는 시작하지 않았다.** 상세 파일 목록과 검증 범위는 [모바일 재계획 인계](HANDOFF_MOBILE_REPLAN.md)를 참고한다. 웹 배포 우선 변경은 DECISIONS IMP-10의 미승인 제안이다.

| A-4 완료 기준 | 실제 확인 결과 |
|---|---|
| 1. 설치 파일 더블클릭 설치·바탕화면 홈 실행 | NSIS 설치 파일·포터블 exe 생성 성공. Windows 앱 접근 승인 시간 초과로 실제 설치·바탕화면 실행은 미검증 |
| 2. 초기 조사 불러오기 후 재시작 유지 | 자동 테스트에서 seed 후 SQLite 연결 종료·재개, 상품 4개와 내보낸 기록 유지 확인. 설치 앱 재시작 조작은 미검증 |
| 3. 첨부 업로드·다운로드, ZIP 내보내기·복원 | 파일 어댑터 왕복 및 기존 API 테스트의 첨부·ZIP 복원 통과. 설치 앱 파일 선택·다운로드 UI는 미검증 |
| 4. 종료 백업 ZIP을 새 설치에 복원해 동일 기록 확인 | 백업 함수 실행 → 독립 빈 SQLite·첨부 폴더 복원 → 기록·원본 이력·첨부 1개 바이트 비교 통과. 실제 창 종료·새 설치 복원은 미검증 |
| 5. 어댑터 포함 테스트·Workers 빌드 | 푸시 전 최종 typecheck·test·build 재실행 통과: 도메인 49개 + Workers API 16개 + 데스크톱 23개(API 16+플랫폼 7) = 88개. desktop:build 타입 검사·패키징도 앞서 통과 |
| 6. IMPLEMENTATION_STATUS·LOCAL_GUIDE 설치 사용법 갱신 | 상태 문서는 실제 결과로 갱신. 설치형을 채택·검증하지 않아 LOCAL_GUIDE의 설치 사용법 전환은 보류 |

`desktop:dev`는 미구현이다. `desktop:backup:verify` CLI는 추가했으나 실제 사용자의 ZIP으로 실행하지 않았다. README·LOCAL_GUIDE의 기존 실행 안내는 기존 웹 개발 환경 기준이며, 현재 데스크톱 산출물의 사용 검증을 의미하지 않는다. 설치 파일·빌드 결과·로컬 캐시는 Git에 포함하지 않는다.

기준: `Yurang2/Seller`, `claude/hopeful-wright-olq2lc`.
읽은 순서: README 구현자 안내 → PLAN → DATA_MODEL → ARCHITECTURE → SEED_RESEARCH → RESEARCH_SHEET.

## 골격 완료 기준

| 기준                              | 상태      | 검증                                                                                                                                              |
| --------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. `pnpm dev` 홈 / health 200     | 로컬 확인 | 홈 200, `/api/v1/health` 200, Vite + Workers + 로컬 D1                                                                                            |
| 2. `0001_core.sql` 로컬·원격 적용 | 확인      | Drizzle 스키마에서 생성한 13개 테이블. 전용 원격 `seller-db` 생성 및 원격/로컬 apply 성공                                                         |
| 3. UI Claim 생성·강등·재조회      | 확인      | 실제 브라우저에서 첨부 없는 확인 가격 입력 → 추정 강등 메시지 → 새로고침 후 같은 값 조회                                                          |
| 4. ZIP → 빈 로컬 DB 복원          | 확인      | Workers API 테스트에서 독립 빈 D1/R2로 Claim·첨부 원본 복원 후 동등성 확인. 실제 개발 서버의 ZIP도 새 로컬 D1으로 restore.ts 미리보기 → 적용 완료 |
| 5. 배포 후 모든 경로 로그인 차단  | 확인      | 2026-09-19 배포 후 `/api/v1/workspace` 401, 위조 쿠키·교차 출처 거절 테스트 통과. 방식은 Access가 아닌 앱 비밀문구(위 2026-09-19 절 참고)                    |
| 6. costing.test.ts 회귀 테스트    | 확인      | 5,900원 고객 청구 배송비를 지출로 사용하면 거절, 미확인 전파, 증빙 강등 등                                                                        |

**M0·M1 로컬 구현 완료. 골격의 원격 Access 기준은 보류.** 사용자 지시 “사이트 마저 구현. 클라우드플레어는 이따 해도 됨”에 따라 로컬 구현을 진행했다(DECISIONS IMP-01~03). 원격 조건을 로컬 테스트로 대체해 통과 표시하지 않는다.
주문·자동화 화면 및 가짜 동기화 상태는 없다. 채널은 원가 계산에 필요한 수수료·정책 기록만 제공하고 외부 연동 상태는 `none`이다. scheduled 핸들러는 작업을 실행하지 않는다.

## M0 검증

- Claim 상태 필수, 증빙 없는 확인 가격의 추정 강등 및 UI 안내.
- 기한 경과 Claim의 홈 표시, 파생 작업의 중복 방지·자동 완료.
- 이유 없는 결정·일반 수정 차단, 변경 전후 이력 및 연결 기록.
- 조사 XLSX 미리보기·행별 결과·해시 확인 후 적용, 같은 파일 반복 적용의 멱등성. CALC-EX1 등 예시 행은 실제 사업 데이터로 가져오지 않는다.
- SEED 상품 4개, 공급처·오퍼·배송 경로·요건·결정·작업·지식 노트 및 용어 설명. D-01 미결정 막힘 유지.
- 기본 마스킹 ZIP(JSON/CSV/첨부), 빈 저장소 복원 검증. 로컬 실제 기록 226개와 원본 변경 이력 315개를 독립 빈 저장소에 복원해 비교했다(2026-09-16 21:53 KST). 이 데이터의 첨부 수는 0개이며, 첨부 바이트 복원은 별도 Workers API 테스트로 검증했다. 실제 비교 성공 후에만 `backup_verified`를 완료했다.

## M1 검증

- 상품 보드, 옵션, 캐릭터·요건 프로필, 공급처·소통, 오퍼 선택 근거, 배송대행·요율표·구간, 준비 체크리스트.
- 미확인 입력은 계산 결과를 차단한다. 추정·범위·환율을 전파하며 고객 청구 배송비는 수입에만 들어간다.
- `templates/research_template.xlsx`의 실제 CALC-EX1 캐시값과 비교: 착지원가 **14,607.86원**, 공헌이익 **9,356.14원**.
- 직배송·배송대행의 개당 항목 비교, 국내 배송 중복 경고, 입력·환율을 고정한 불변 원가 스냅샷 및 “현재 값과 다름” 표시.
- 경쟁사 관찰만으로 요건 통과 불가, fail 게이트의 가격 단계 차단. 가격 결정은 이유·대안·재검토 조건을 남기며 준비 미완료 시 판매 준비 단계로 이동할 수 없다.
- 브라우저에서 초기 자료 등록, 상품별 경로 비교, 노트 저장·새로고침 유지, 홈 반영, 390px 모바일 화면을 확인했다.

스키마는 28개 테이블이며 `0002_research.sql`은 로컬에만 적용했다. 2026-09-16 최종 검사에서 `pnpm test` 37개(도메인 25, Workers API 12)와 `pnpm build`(타입 검사 포함)가 통과했다. 검사 명세는 `tests/domain`, `tests/api`에 있다.

## 원격 검증 전 필요한 설정

현재 환경의 Cloudflare 토큰으로 인증 및 D1 목록·Access 앱 목록 조회는 가능했다.
Workers subdomain, Access 조직, R2 목록 API는 403(Authentication error)을 반환했다.
Access 앱 목록은 비어 있었다. 전용 DB `seller-db`를 생성하고 `0001_core.sql`을 원격 적용했다.
증빙 버킷 생성은 Authentication error(10000)로 실패했다. 버킷과 Worker는 생성하지 않았다.

- 사용하려는 계정의 Workers 배포, D1 생성·수정, R2 생성·수정, Access 앱·정책, 조직 설정에 필요한 권한/설정을 준비한다.
- Zero Trust 팀 도메인과 허용할 사용자 이메일을 확정한다.
- 원격 D1의 실제 `database_id`는 wrangler.jsonc에 반영했다. 나머지 원격 리소스와 `ACCESS_TEAM_DOMAIN`, `ACCESS_AUD` 설정이 필요하다.
- 도메인 전체의 Access 보호를 설정하고 배포 후 `/`, 정적 파일, 미등록 SPA 경로, `/api/v1/health`의 비로그인 차단을 확인한다.
- 비밀 값은 대화나 Git에 저장하지 않는다.

## 로컬 개발

```sh
pnpm install
# .dev.vars.example을 .dev.vars로 복사 (로컬 APP_ENV=development)
pnpm db:migrate:local
pnpm dev
pnpm test
pnpm build
```

이 Windows 환경에서는 workerd의 Documents 하위 영속 DB 접근이 실패했다.
선택적 `.env.local`의 `SELLER_LOCAL_STATE_DIR`를 앱 데이터 폴더로 지정하면 정상 동작한다.
Vite와 마이그레이션 스크립트는 같은 값을 사용한다. 기본값은 기존 `.wrangler/state` 동작이다.
로컬 개인 경로와 데이터는 Git에서 제외한다. 운영 배포의 D1/R2 위치에는 영향이 없다.

`@cloudflare/vitest-pool-workers`의 Vitest 4.1 호환성을 고정했다.
동봉된 테스트 workerd가 문서의 compatibility_date보다 오래돼 workerd 1.20260916.1로 고정했다.
패키지와 lockfile을 함께 보관한다.

## 남은 범위

1. ~~Cloudflare R2/Worker 설정, 원격 마이그레이션, 로그인 보호 배포 검증~~ → 2026-09-19 완료(위 절). 남은 것: 서버 백업 Job(C-2), 모바일 화면(M), 표 화면(B).
2. PLAN M2 이후의 판매 채널 등록, 주문·정산·반품 운영 및 실제 외부 자동화. 이번 로컬 M0·M1 범위에는 포함하지 않는다.
3. 사업 모델, 상품 권리·안전 요건, 실제 견적·환율 등 미결정 정보는 사용자 조사·판단으로 채운다. 샘플 계산값을 실제 견적으로 자동 입력하지 않는다.

사용 흐름과 로컬 실행·백업 방법은 `LOCAL_GUIDE.md`를 참고한다.

## 2026-09-18 갱신 · 검토 보고(REVIEW_2026-09-18.md) 반영과 M2

- P1: 요건 판정 근거 강도(위험도 높음 → 확인 답변 + 첨부/링크, 가정 답변으로 통과 불가), 첨부 소유자·유형 검증(같은 기록의 이미지·PDF만), 조사→원가확인은 실결제가 근거의 캡처 필요. `tests/api/m1.test.ts`가 실패 경로를 검증한다.
- P2: 범위 금액도 최소 단위 규칙 적용, 환율 기준일 1일 초과 시 결과 추정 강등(`fx_age_days`, 안내 문구), 미확인 자리표시 값은 신선도·재확인 작업 대상에서 제외, 엑셀 가져오기가 무시한 상품 열을 "안내" 행으로 보고.
- P3: 홈에 막힘 상세(사유·경과일·해제 조건·재확인일), 다음 행동 10개, 가장 오래 머문 상품, 판매 개시까지 남은 것, 돈(스냅샷 예상), 모드 표시. 채널 화면에 "연동 안 됨 + 수동 절차 링크" 배너. wrangler cron 제거. 소프트 삭제 복구(API·UI). 삭제된 소유자의 근거는 목록에서 숨김.
- P4: `Foundation.tsx`·`domain/costing.ts` 삭제. DATA_MODEL·PLAN·DECISIONS(IMP-04~07) 동기화.
- M2: `listings` 테이블(0003_listings.sql)·기록 화면, 판매 중 조건(채널 상품 번호·결정 판매가 일치·이미지 출처·노출 확인일), 상품 `listing_ready→live`, `live↔paused`, 파생 작업 `product_needs_listing`, 수동 절차 SOP 2종 자동 생성, 운영 모드 판정. `tests/api/m2.test.ts`.
- 검증: `pnpm typecheck`, `pnpm test`(도메인 46 + Workers API 16), `pnpm build`, 로컬 화면 확인.
- 여전히 없는 것: 주문·정산(M3), 외부 연동(M4). (Cloudflare 배포는 2026-09-19 완료)
