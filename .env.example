# Optional local-only storage path if Windows blocks workerd in Documents.
# Copy to .env.local and set a persistent directory, not a temporary directory.
# SELLER_LOCAL_STATE_DIR=C:/Users/<user>/AppData/Local/Seller/state
